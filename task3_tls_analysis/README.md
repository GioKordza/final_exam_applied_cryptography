# Task 3: TLS Communication Inspection & Analysis

This task inspects a live TLS connection using OpenSSL and analyzes the handshake.

## Files

- `openssl_output.png` – Screenshot from OpenSSL TLS connection to google.com
- `tls_summary.txt` – Text summary of findings: TLS version, cipher suite, cert chain, and security principles

## Tools Used

- OpenSSL: Inspect certificate and cipher via `openssl s_client -connect google.com:443`
- Wireshark: (To be added by student with handshake screenshots)
ECHO is on.
